import GalleryView from "@/components/sections/GalleryView";

export const metadata = {
  title: "Gallery",
  description:
    "Photos from the Nova Solutions office, events, workshops and team activities.",
};

export default function GalleryPage() {
  return <GalleryView />;
}